import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import EvidenceUpload from "@/components/EvidenceUpload";
import SearchableMultiSelect from "@/components/SearchableMultiSelect";
import { useAuth } from "@/_core/hooks/useAuth";
import { useEffectivePermissions } from "@/hooks/useEffectivePermissions";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CalendarDays, ClipboardCheck, Handshake, MapPinned, PackageCheck, Plus, RefreshCw, Search, Star, UsersRound } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "sonner";

type StockAllocation = { stockItemId: number; quantity: string };
const statusLabel: Record<string, string> = { planned: "Planejado", in_progress: "Em execução", completed: "Concluído", cancelled: "Cancelado" };
const partnershipLabel: Record<string, string> = { paid: "Pago", barter: "Permuta", mixed: "Misto" };

export default function EventsWorkspace() {
  const { user } = useAuth();
  const { can } = useEffectivePermissions();
  const canWrite = can("events.write");
  const utils = trpc.useUtils();
  const references = trpc.events.referenceData.useQuery();
  const eventList = trpc.events.list.useQuery();
  const initialForm = { name: "", cityId: "", eventTypeId: "", actionPointId: "", startsAt: "", endsAt: "", address: "", commercialSupervisorId: "", partnershipType: "paid" as "paid" | "barter" | "mixed", estimatedCost: "0", partnershipReason: "", preEventNotes: "", supplierIds: [] as number[], serviceTypeIds: [] as number[], teamMemberIds: [] as number[], stockAllocations: [] as StockAllocation[] };
  const [form, setForm] = useState(initialForm);
  const [isFormOpen, setIsFormOpen] = useState(false);
  const [postEventId, setPostEventId] = useState<number | null>(null);
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [search, setSearch] = useState("");
  const [statusFilter, setStatusFilter] = useState("all");
  const [post, setPost] = useState({ postEventNotes: "", rating: "5", resultAchieved: true, worthRenewing: true, status: "completed" as "planned" | "in_progress" | "completed" | "cancelled" });
  const cityOptions = (references.data?.cities ?? []).map((entry: any) => ({ city: entry.city ?? entry, regionalName: entry.regionalName ?? entry.city?.regionalName ?? "" }));
  const selectedCity = cityOptions.find(({ city }) => city.id === Number(form.cityId))?.city;
  const supplierOptions = useMemo(() => !form.cityId ? [] : (references.data?.suppliers ?? []).filter(supplier => (references.data?.supplierCities ?? []).some(link => link.supplierId === supplier.id && link.cityId === Number(form.cityId))).map(supplier => ({ id: supplier.id, label: supplier.displayName })), [references.data, form.cityId]);
  const stockOptions = useMemo(() => !selectedCity ? [] : (references.data?.stockItems ?? []).filter(item => item.regionalId === selectedCity.regionalId && (item.cityId === null || item.cityId === selectedCity.id)).map(item => ({ id: item.id, label: item.name, description: `${item.sku} · ${item.unit}` })), [references.data, selectedCity]);
  const pointOptions = useMemo(() => !form.cityId ? [] : (references.data?.actionPoints ?? []).filter(point => point.active && point.cityId === Number(form.cityId)).map(point => ({ id: point.id, label: point.name, description: point.address || "Sem endereço cadastrado" })), [references.data, form.cityId]);
  const visibleEvents = useMemo(() => (eventList.data ?? []).filter(row => (statusFilter === "all" || row.event.status === statusFilter) && `${row.event.name} ${row.cityName} ${row.eventTypeName}`.toLocaleLowerCase("pt-BR").includes(search.toLocaleLowerCase("pt-BR"))), [eventList.data, search, statusFilter]);
  const selectedEvent = (eventList.data ?? []).find(row => row.event.id === selectedId);

  const create = trpc.events.create.useMutation({
    onSuccess: () => { toast.success("Evento planejado com sucesso."); utils.events.list.invalidate(); setIsFormOpen(false); setForm(initialForm); },
    onError: error => toast.error(error.message),
  });
  const savePost = trpc.events.savePostEvent.useMutation({
    onSuccess: () => { toast.success("Pós-evento registrado."); utils.events.list.invalidate(); setPostEventId(null); },
    onError: error => toast.error(error.message),
  });

  const submit = (event: FormEvent) => {
    event.preventDefault();
    create.mutate({
      name: form.name, cityId: Number(form.cityId), eventTypeId: Number(form.eventTypeId), startsAt: new Date(form.startsAt), endsAt: form.endsAt ? new Date(form.endsAt) : null,
      address: form.address || undefined, latitude: null, longitude: null, commercialSupervisorId: form.commercialSupervisorId ? Number(form.commercialSupervisorId) : null,
      partnershipType: form.partnershipType, estimatedCost: Number(form.estimatedCost), partnershipReason: form.partnershipReason || undefined, preEventNotes: form.preEventNotes || undefined,
      supplierIds: form.supplierIds, serviceTypeIds: form.serviceTypeIds, teamMemberIds: form.teamMemberIds,
      stockAllocations: form.stockAllocations.map(item => ({ stockItemId: item.stockItemId, quantity: Number(item.quantity) })),
    });
  };
  const updateStockSelection = (stockItemIds: number[]) => setForm(current => ({ ...current, stockAllocations: stockItemIds.map(stockItemId => current.stockAllocations.find(item => item.stockItemId === stockItemId) ?? { stockItemId, quantity: "1" }) }));

  return <main className="mx-auto max-w-[1480px]">
    <header className="flex flex-col gap-5 border-b border-border pb-6 sm:flex-row sm:items-end sm:justify-between">
      <div className="flex gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-primary text-primary-foreground shadow-sm"><MapPinned className="h-5 w-5" /></span><div><p className="text-[11px] font-semibold uppercase tracking-[0.14em] text-primary">Experiências presenciais</p><h1 className="mt-1 font-display text-3xl font-semibold tracking-tight text-foreground">Eventos</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Planejamento territorial, equipe, fornecedores, recursos, custos e avaliação da parceria em uma única operação.</p></div></div>
      {canWrite && <Button onClick={() => setIsFormOpen(true)} className="h-10 rounded-xl bg-primary px-4 text-xs font-semibold hover:bg-primary/90"><Plus className="mr-1.5 h-4 w-4" /> Novo evento</Button>}
    </header>

    <Dialog open={isFormOpen} onOpenChange={setIsFormOpen}>
      <DialogContent className="max-h-[88vh] w-[calc(100vw-2rem)] max-w-6xl overflow-y-auto p-6 sm:p-7">
        <DialogHeader><DialogTitle>Planejar novo evento</DialogTitle><DialogDescription>Escolha primeiro a cidade. O sistema então protege a coerência territorial de fornecedores e recursos de estoque.</DialogDescription></DialogHeader>
        <form onSubmit={submit} className="mt-2 grid gap-4 md:grid-cols-4">
          <Field label="Nome do evento" htmlFor="event-name"><Input id="event-name" required value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} /></Field>
          <Field label="Cidade e regional" htmlFor="event-city"><select id="event-city" required value={form.cityId} onChange={event => setForm(current => ({ ...current, cityId: event.target.value, actionPointId: "", address: "", supplierIds: [], stockAllocations: [] }))} className="control"><option value="">Selecionar cidade</option>{cityOptions.map(({ city, regionalName }) => <option key={city.id} value={city.id}>{regionalName ? `${regionalName} · ` : ""}{city.name}/{city.state}</option>)}</select><p className="helper">Define os vínculos territoriais.</p></Field>
          <Field label="Tipo de evento" htmlFor="event-type"><select id="event-type" required value={form.eventTypeId} onChange={event => setForm({ ...form, eventTypeId: event.target.value })} className="control"><option value="">{references.data?.eventTypes.length ? "Selecionar" : "Nenhum tipo cadastrado"}</option>{references.data?.eventTypes.map(type => <option key={type.id} value={type.id}>{type.name}</option>)}</select>{!references.isLoading && !references.data?.eventTypes.length ? <p className="helper text-destructive">Cadastre um Tipo de evento em Cadastros para planejar.</p> : null}</Field>
          <Field label="Supervisor comercial" htmlFor="event-supervisor"><select id="event-supervisor" value={form.commercialSupervisorId} onChange={event => setForm({ ...form, commercialSupervisorId: event.target.value })} className="control"><option value="">Não definido</option>{references.data?.supervisors.map(supervisor => <option key={supervisor.id} value={supervisor.id}>{supervisor.name}</option>)}</select></Field>
          <Field label="Início" htmlFor="event-start"><Input id="event-start" required type="datetime-local" value={form.startsAt} onChange={event => setForm({ ...form, startsAt: event.target.value })} /></Field>
          <Field label="Término" htmlFor="event-end"><Input id="event-end" type="datetime-local" value={form.endsAt} onChange={event => setForm({ ...form, endsAt: event.target.value })} /></Field>
          <Field label="Modalidade" htmlFor="event-partnership"><select id="event-partnership" value={form.partnershipType} onChange={event => setForm({ ...form, partnershipType: event.target.value as typeof form.partnershipType })} className="control"><option value="paid">Pago</option><option value="barter">Permuta</option><option value="mixed">Misto</option></select></Field>
          <Field label="Custo previsto (R$)" htmlFor="event-cost"><Input id="event-cost" required min="0" step="0.01" type="number" value={form.estimatedCost} onChange={event => setForm({ ...form, estimatedCost: event.target.value })} /></Field>
          <div className="md:col-span-2"><SearchableMultiSelect id="event-point" label="Ponto comercial ou local de ação" options={pointOptions} values={form.actionPointId ? [Number(form.actionPointId)] : []} disabled={!form.cityId} placeholder="Selecionar ponto cadastrado" onChange={ids => { const point = (references.data?.actionPoints ?? []).find(item => item.id === ids.at(-1)); setForm(current => ({ ...current, actionPointId: ids.at(-1) ? String(ids.at(-1)) : "", address: point?.address ?? current.address })); }} emptyMessage="Nenhum ponto cadastrado para esta cidade." /></div>
          <div className="md:col-span-2"><Label htmlFor="event-address">Endereço e ponto de referência</Label><Input id="event-address" value={form.address} onChange={event => setForm({ ...form, address: event.target.value })} className="mt-1.5" placeholder="Preenchido pelo ponto selecionado, se houver." /></div>
          <div className="md:col-span-2"><Label htmlFor="event-reason">Motivo da parceria</Label><Textarea id="event-reason" value={form.partnershipReason} onChange={event => setForm({ ...form, partnershipReason: event.target.value })} className="mt-1.5 min-h-24" /></div>
          <div className="md:col-span-2"><Label htmlFor="event-pre">Entregáveis e observações pré-evento</Label><Textarea id="event-pre" value={form.preEventNotes} onChange={event => setForm({ ...form, preEventNotes: event.target.value })} className="mt-1.5 min-h-24" /></div>
          <div className="md:col-span-2"><SearchableMultiSelect id="event-team" label="Responsáveis do trade" options={(references.data?.teamUsers ?? []).map(member => ({ id: member.id, label: member.name || member.email || `Usuário #${member.id}`, description: member.jobTitle || undefined }))} values={form.teamMemberIds} onChange={ids => setForm({ ...form, teamMemberIds: ids })} /></div>
          <div className="md:col-span-2"><SearchableMultiSelect id="event-suppliers" label="Fornecedores envolvidos" options={supplierOptions} values={form.supplierIds} disabled={!form.cityId} onChange={ids => setForm({ ...form, supplierIds: ids })} emptyMessage="Nenhum fornecedor atende esta cidade." /></div>
          <div className="md:col-span-2"><SearchableMultiSelect id="event-services" label="Serviços" options={(references.data?.serviceTypes ?? []).map(service => ({ id: service.id, label: service.name }))} values={form.serviceTypeIds} onChange={ids => setForm({ ...form, serviceTypeIds: ids })} /></div>
          <div className="md:col-span-2"><SearchableMultiSelect id="event-stock" label="Recursos de estoque" options={stockOptions} values={form.stockAllocations.map(item => item.stockItemId)} disabled={!form.cityId} onChange={updateStockSelection} emptyMessage="Nenhum recurso está disponível para esta cidade." /></div>
          {form.stockAllocations.length > 0 && <div className="md:col-span-4 rounded-xl border border-border bg-muted/50 p-4"><p className="text-xs font-semibold text-foreground">Quantidade planejada por recurso</p><div className="mt-3 grid gap-3 sm:grid-cols-2">{form.stockAllocations.map(allocation => { const item = references.data?.stockItems.find(stock => stock.id === allocation.stockItemId); return <label key={allocation.stockItemId} className="flex items-center gap-2 text-xs text-foreground"><span className="min-w-0 flex-1 truncate">{item?.name}</span><Input aria-label={`Quantidade planejada para ${item?.name ?? allocation.stockItemId}`} required min="0.01" step="0.01" type="number" value={allocation.quantity} onChange={event => setForm(current => ({ ...current, stockAllocations: current.stockAllocations.map(stock => stock.stockItemId === allocation.stockItemId ? { ...stock, quantity: event.target.value } : stock) }))} className="h-8 w-24" /><span>{item?.unit}</span></label>; })}</div></div>}
          <div className="flex justify-end gap-2 md:col-span-4"><Button type="button" variant="outline" onClick={() => setIsFormOpen(false)}>Cancelar</Button><Button type="submit" disabled={create.isPending} className="bg-primary hover:bg-primary/90">{create.isPending && <RefreshCw className="mr-2 h-4 w-4 animate-spin" />}Planejar evento</Button></div>
        </form>
      </DialogContent>
    </Dialog>

    {selectedEvent ? <EventDetail row={selectedEvent} canWrite={canWrite} post={post} setPost={setPost} pending={savePost.isPending} onBack={() => setSelectedId(null)} onSave={() => savePost.mutate({ eventId: selectedEvent.event.id, postEventNotes: post.postEventNotes || undefined, rating: Number(post.rating), resultAchieved: post.resultAchieved, worthRenewing: post.worthRenewing, status: post.status })} /> : <section className="mt-6 overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="flex flex-col gap-3 border-b border-border p-4 lg:flex-row lg:items-center lg:justify-between"><div className="relative min-w-0 flex-1"><Search className="pointer-events-none absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><Input value={search} onChange={event => setSearch(event.target.value)} placeholder="Pesquisar por evento, cidade ou tipo…" className="pl-9" /></div><div className="flex flex-wrap gap-2">{[["all", "Todos"], ["planned", "Planejados"], ["in_progress", "Em execução"], ["completed", "Concluídos"], ["cancelled", "Cancelados"]].map(([value, label]) => <Button key={value} type="button" size="sm" variant={statusFilter === value ? "default" : "outline"} className={statusFilter === value ? "bg-primary" : ""} onClick={() => setStatusFilter(value)}>{label}</Button>)}</div></div><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 className="font-display text-lg font-semibold text-foreground">Agenda e avaliação</h2><p className="mt-0.5 text-xs text-muted-foreground">Abra um evento para consultar a ficha completa, evidências e debriefing.</p></div><Badge variant="outline">{visibleEvents.length} eventos</Badge></div>
      {eventList.isLoading ? <div className="p-10 text-center text-sm text-muted-foreground">Carregando eventos...</div> : visibleEvents.length ? <div className="divide-y divide-border">{visibleEvents.map(({ event, cityName, eventTypeName, supervisorName, teamMembers, stockItems }) => <article key={event.id} onClick={() => setSelectedId(event.id)} className="cursor-pointer px-5 py-4 transition hover:bg-muted/50"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-foreground">{event.name}</p><Badge className="bg-secondary text-secondary-foreground">{statusLabel[event.status]}</Badge><Badge variant="outline">{partnershipLabel[event.partnershipType]}</Badge></div><p className="mt-1 text-xs text-muted-foreground">{eventTypeName} · {cityName} · {new Date(event.startsAt).toLocaleString("pt-BR")}{event.endsAt ? ` até ${new Date(event.endsAt).toLocaleString("pt-BR")}` : ""}</p><div className="mt-2 flex flex-wrap gap-x-4 gap-y-1 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1"><Handshake className="h-3.5 w-3.5" /> {Number(event.estimatedCost).toLocaleString("pt-BR", { style: "currency", currency: "BRL" })}</span>{supervisorName && <span>Supervisor: {supervisorName}</span>}{teamMembers.length > 0 && <span className="inline-flex items-center gap-1"><UsersRound className="h-3.5 w-3.5" /> {teamMembers.map(member => member.name || `Usuário #${member.userId}`).join(", ")}</span>}{stockItems.length > 0 && <span className="inline-flex items-center gap-1"><PackageCheck className="h-3.5 w-3.5" /> {stockItems.length} recurso(s)</span>}</div>{event.rating && <p className="mt-2 inline-flex items-center gap-1 text-xs font-medium text-primary"><Star className="h-3.5 w-3.5 fill-current" /> Nota {event.rating}/5{event.worthRenewing ? " · Recomendado renovar" : " · Avaliar renovação"}</p>}</div>{canWrite && <Button variant="outline" size="sm" className="shrink-0" onClick={click => { click.stopPropagation(); setPostEventId(postEventId === event.id ? null : event.id); }}><ClipboardCheck className="mr-1.5 h-3.5 w-3.5" /> Pós-evento</Button>}</div>
        {postEventId === event.id && <form onSubmit={formEvent => { formEvent.preventDefault(); savePost.mutate({ eventId: event.id, postEventNotes: post.postEventNotes || undefined, rating: Number(post.rating), resultAchieved: post.resultAchieved, worthRenewing: post.worthRenewing, status: post.status }); }} className="mt-4 grid gap-3 rounded-xl border border-border bg-muted/50 p-4 md:grid-cols-4"><Field label="Status" htmlFor={`event-status-${event.id}`}><select id={`event-status-${event.id}`} value={post.status} onChange={input => setPost({ ...post, status: input.target.value as typeof post.status })} className="control"><option value="in_progress">Em execução</option><option value="completed">Concluído</option><option value="cancelled">Cancelado</option></select></Field><Field label="Nota" htmlFor={`event-rating-${event.id}`}><select id={`event-rating-${event.id}`} value={post.rating} onChange={input => setPost({ ...post, rating: input.target.value })} className="control">{[5, 4, 3, 2, 1].map(rating => <option key={rating} value={rating}>{rating} estrela{rating > 1 ? "s" : ""}</option>)}</select></Field><label className="flex items-center gap-2 pt-6 text-xs text-foreground"><input type="checkbox" checked={post.resultAchieved} onChange={input => setPost({ ...post, resultAchieved: input.target.checked })} /> Resultado atingido</label><label className="flex items-center gap-2 pt-6 text-xs text-foreground"><input type="checkbox" checked={post.worthRenewing} onChange={input => setPost({ ...post, worthRenewing: input.target.checked })} /> Vale renovar</label><div className="md:col-span-4"><Label htmlFor={`event-post-${event.id}`}>Debriefing</Label><Textarea id={`event-post-${event.id}`} value={post.postEventNotes} onChange={input => setPost({ ...post, postEventNotes: input.target.value })} className="mt-1.5" placeholder="Registre aprendizados, entregáveis e recomendações." /></div><div className="flex justify-end md:col-span-4"><Button size="sm" type="submit" disabled={savePost.isPending} className="bg-primary hover:bg-primary/90">Salvar avaliação</Button></div></form>}</article>)}</div> : <div className="p-10 text-center text-sm text-muted-foreground">Nenhum evento registrado. Planeje o primeiro evento para iniciar o acompanhamento.</div>}
    </section>}</main>;
}

function Field({ label, htmlFor, children }: { label: string; htmlFor: string; children: React.ReactNode }) { return <div><Label htmlFor={htmlFor}>{label}</Label><div className="mt-1.5">{children}</div></div>; }

function EventDetail({ row, canWrite, post, setPost, pending, onBack, onSave }: { row: any; canWrite: boolean; post: any; setPost: (value: any) => void; pending: boolean; onBack: () => void; onSave: () => void }) {
  const event = row.event;
  return <section className="mt-6"><Button variant="outline" onClick={onBack} className="mb-5"><ArrowLeft className="mr-2 h-4 w-4" />Voltar à lista</Button><header className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex flex-wrap gap-2"><Badge variant="outline">{statusLabel[event.status]}</Badge><Badge className="bg-secondary text-secondary-foreground">{partnershipLabel[event.partnershipType]}</Badge></div><h2 className="mt-3 font-display text-3xl font-semibold text-foreground">{event.name}</h2><p className="mt-2 text-sm text-muted-foreground">{row.eventTypeName} · {row.cityName} · {new Date(event.startsAt).toLocaleString("pt-BR")}{event.endsAt ? ` até ${new Date(event.endsAt).toLocaleString("pt-BR")}` : ""}</p></header><div className="mt-5 grid gap-5 lg:grid-cols-3"><div className="space-y-5 lg:col-span-2"><EventDetailSection title="Planejamento e local"><div className="grid gap-3 sm:grid-cols-2"><EventDetailValue label="Localização" value={event.address || "Não informada"} /><EventDetailValue label="Custo previsto" value={Number(event.estimatedCost).toLocaleString("pt-BR", { style: "currency", currency: "BRL" })} /><EventDetailValue label="Supervisor" value={row.supervisorName || "Não definido"} /><EventDetailValue label="Motivo da parceria" value={event.partnershipReason || "Não informado"} /></div><p className="mt-4 text-sm leading-6 text-foreground">{event.preEventNotes || "Sem observações pré-evento registradas."}</p></EventDetailSection><EventDetailSection title="Pós-evento, debriefing e resultado">{canWrite ? <form onSubmit={formEvent => { formEvent.preventDefault(); onSave(); }} className="grid gap-3 md:grid-cols-2"><Field label="Status" htmlFor={`detail-event-status-${event.id}`}><select id={`detail-event-status-${event.id}`} value={post.status} onChange={input => setPost({ ...post, status: input.target.value })} className="control"><option value="in_progress">Em execução</option><option value="completed">Concluído</option><option value="cancelled">Cancelado</option></select></Field><Field label="Nota" htmlFor={`detail-event-rating-${event.id}`}><select id={`detail-event-rating-${event.id}`} value={post.rating} onChange={input => setPost({ ...post, rating: input.target.value })} className="control">{[5,4,3,2,1].map(rating => <option key={rating} value={rating}>{rating} estrelas</option>)}</select></Field><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={post.resultAchieved} onChange={input => setPost({ ...post, resultAchieved: input.target.checked })} />Resultado atingido</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={post.worthRenewing} onChange={input => setPost({ ...post, worthRenewing: input.target.checked })} />Vale renovar</label><div className="md:col-span-2"><Label htmlFor={`detail-event-post-${event.id}`}>Debriefing</Label><Textarea id={`detail-event-post-${event.id}`} value={post.postEventNotes} onChange={input => setPost({ ...post, postEventNotes: input.target.value })} className="mt-1.5" placeholder="Registre aprendizados, entregáveis e recomendações." /></div><div className="flex justify-end md:col-span-2"><Button type="submit" disabled={pending} className="bg-primary">Salvar avaliação</Button></div></form> : <p className="text-sm text-muted-foreground">{event.postEventNotes || "Nenhum debriefing registrado."}</p>}</EventDetailSection><EventDetailSection title="Equipe e recursos"><div className="grid gap-3 sm:grid-cols-2"><EventDetailValue label="Responsáveis" value={row.teamMembers.length ? row.teamMembers.map((member: any) => member.name || `Usuário #${member.userId}`).join(", ") : "Não informados"} /><EventDetailValue label="Recursos de estoque" value={row.stockItems.length ? row.stockItems.map((item: any) => `${item.name}: ${item.plannedQuantity} ${item.unit}`).join(" · ") : "Não informados"} /></div></EventDetailSection></div><aside><EventDetailSection title="Fotos, vídeos e evidências"><EvidenceUpload entityType="event" entityId={event.id} canWrite={canWrite} /></EventDetailSection></aside></div></section>;
}

function EventDetailSection({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h3 className="font-display text-lg font-semibold text-foreground">{title}</h3><div className="mt-4">{children}</div></section>; }
function EventDetailValue({ label, value }: { label: string; value: string }) { return <div className="rounded-xl bg-muted/60 p-3"><p className="text-xs font-semibold text-muted-foreground">{label}</p><p className="mt-1 text-sm text-foreground">{value}</p></div>; }
